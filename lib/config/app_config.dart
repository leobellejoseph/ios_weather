import 'package:flutter/material.dart';

abstract class AppConfig {
  static Color backgroundColor = Color(0xFF315D9A).withOpacity(0.9);
  static const bodyPadding = EdgeInsets.only(left: 10, right: 10);
  static const padding = EdgeInsets.only(left: 10, right: 10);
}